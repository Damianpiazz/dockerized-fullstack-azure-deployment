# ─── Frontend — Production ─────────────────────────────
# Template. Los valores ${VAR} los injecta CI/CD o el .env en la VM.
NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
NEXT_PUBLIC_OTEL_ENABLED=true
